export enum Asset {
    BACKGROUND = 'background',
    GROUND = 'ground',
    BIRD = 'bird',
    PIPE = 'pipe',
    MUSIC = 'music',
    JUMP = 'jump',
    HURT = 'hurt',
    CRASH = 'crash',
    SCORE = 'score',
}
